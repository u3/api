# Provider keys (never commit real values)
OPTICODDS_API_KEY=
ODDSPAPI_API_KEY=
SHARPSPORTS_API_KEY=          # public key
SHARPSPORTS_API_SECRET=       # private key (required for /events, /prices, historicData)
# Storage
U3_RAW_DIR=./data/raw         # raw JSONL.gz archive root
U3_GCS_BUCKET=                # optional: gs bucket for raw archive upload
U3_CLICKHOUSE_URL=            # optional: https://user:pass@host:8443/db
# LLM research tooling (optional)
ZENMUX_API_KEY=
ZENMUX_PLATFORM_API=
